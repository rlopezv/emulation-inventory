# Scripts

Scripts del pipeline de romsets: indexado de DATs, construcción/promoción de sets completos, filtrado y generación de documentación. La mayoría son PowerShell (`pwsh tools/scripts/<script>.ps1`, entorno Windows nativo); algunos tienen además un equivalente Python (`python tools/scripts/<script>.py`, pensado para ejecutarse desde el devcontainer — ver `.devcontainer/`). Ambos caminos son válidos, ninguno sustituye al otro; el `.py` se valida contrastando su salida contra el `.ps1` correspondiente antes de darlo por bueno. Ver la cabecera de cada fichero para parámetros y ejemplos de uso completos.

## Sincronización de fuentes de trabajo (`sources/`)

`metadata/dat/<Fuente>/` es el archivo crudo (todo lo que se descarga, tal cual). `sources/dats/<fuente>/` es la copia de trabajo curada que leen los scripts de indexado — solo los sistemas realmente usados, sin duplicados por timestamp. Ejecutar antes de (re)indexar tras una descarga nueva.

| Script | Propósito |
| --- | --- |
| `update-sources.ps1` / `update-sources.py` | Sincroniza `sources/dats/no-intro/{pc,full,aftermarket}/` desde `metadata/dat/No-Intro/{pc,full,aftermarket}/`, según el manifiesto `config/nointro-systems.json` (id canónico → nombre base del DAT). Copia el fichero más reciente que coincida por sistema/pack, y avisa de sistemas sin descarga o de ficheros huérfanos en `sources/` que ya no están en el manifiesto. Equivalentes verificados: misma salida para el manifiesto completo. |

## Indexado de DAT (`metadata/dat-index/`)

| Script | Propósito |
| --- | --- |
| `build-dat-index-nointro.ps1` / `build-dat-index-nointro.py` | Genera `metadata/dat-index/<id>.json` a partir de DAT No-Intro (`sources/dats/no-intro/{full,aftermarket,pc}/`, resueltos por nombre base vía `config/nointro-systems.json`) o Non-Redump (nombre de fichero exacto en `metadata/dat/Non-Redump/`); agrupa familias 1G1R por relación parent/clone. Fuente del árbol seleccionable con `-SourceMode`/`--source-mode`: `FullAftermarket` (por defecto, `full`+`aftermarket` combinados vía `cloneofid`), `Pc` (árbol completo desde `pc/`, vía `cloneof` por nombre) o `Merged` (`FullAftermarket` completado con lo detectado solo en `pc/`). Genera además, si hay pack `pc/`, un informe de contraste bidireccional (`debug/<id>-pc-contrast.json`) y, si hay clonelist de retool, un informe de divergencias (`debug/<id>-clonelist-diff.json`). Equivalentes verificados por contenido (conjunto de familias) en 33 de 34 sistemas; NES tiene una discrepancia residual conocida, causada por una ambigüedad pre-existente del algoritmo de fusión de alias manual ante colisiones de nombre canónico, no por la portabilidad en sí. |
| `build-dat-index-redump.ps1` / `build-dat-index-redump.py` | Igual que el anterior pero para DAT Redump (sin `cloneofid`); agrupa por clonelist o, si no existe, por nombre base exacto. Redump sigue la misma convención de nombrado que No-Intro (verificado: 99.6% cobertura de región, 98.8% coincidencia con metadata de retool en PSX), así que comparte el mismo diseño de `languages`/`members[]`/`revision`/`version`/`isAlt` y validación posterior contra `retool-clonelists-metadata` (`debug/<id>-language-diff.json`). Pendiente: tratamiento de multi-disco (`(Disc N)`) como caso distinto de revisión/alt — sin campo dedicado todavía. |
| `build-dat-index-tosec.ps1` | Igual que los anteriores pero para DAT TOSEC (sin `cloneofid`, convención de nombre con código de región de 2 letras). |
| `build-dat-index-1g1r.ps1` | Genera el índice a partir de los DAT ya curados 1G1R de `data/dats/console/1g1r/` (generados externamente con retool); no necesita agrupar por parent/clone, retool ya lo resolvió. |
| `inspect-dat-index.ps1` | Muestra un resumen de validación de un `metadata/dat-index/<id>.json` (total de familias, entradas sin región, distribución de categorías, muestra de alias). |
| `filter-by-title-type.ps1` | Limpia un índice dejando solo familias con `<game_id>` de un prefijo de Title ID permitido (uso: catálogos digitales Nintendo 3DS/DSi). |
| `filter-cross-system-duplicates.ps1` | Limpia un índice eliminando familias ya cubiertas por otro sistema relacionado (uso: `3dseshop` vs `3ds`). |
| `find-1g1r-duplicates.ps1` | Detecta nombres de familia duplicados dentro de un índice 1G1R generado por `build-dat-index-1g1r.ps1`. |

## Validación cruzada entre fuentes

| Script | Propósito |
| --- | --- |
| `compare-redump-mameredump.py` | Compara, por título completo (no por hash — no son comparables entre sí), el DAT oficial de Redump (`metadata/dat/Redump/`) contra el equivalente de `metadata/dat/MAMERedump/full/` (DAT estilo MAME con el SHA1 real del CHD, `github.com/MetalSlug/MAMERedump`) para los sistemas listados en `config/redump-mameredump-systems.json`. Soporta tanto el esquema `<machine>/<disk .chd>` (la mayoría) como `<game>/<rom>` estándar (GameCube/Wii, que usan `.rvz` en vez de CHD). Genera `metadata/dat-index/debug/<id>-redump-mameredump-diff.json` por sistema (títulos solo en Redump / solo en MAMERedump); puramente informativo, no bloquea nada ni modifica ningún DAT. `xbox` queda fuera del manifiesto a propósito: Redump lo cubre pero MAMERedump no publica CHD para él. |

## Conversión de esquema DAT

| Script | Propósito |
| --- | --- |
| `convert-cloneofid-to-parent-clone.ps1` / `convert-cloneofid-to-parent-clone.py` | Conversor genérico e independiente (no usa `dat-index` ni el manifiesto de sistemas): recibe uno o varios DAT Logiqx en esquema Standard (`id`/`cloneofid`, ej. `full`+`aftermarket` de No-Intro) y devuelve un único DAT fusionado en esquema Parent-Clone (`cloneof` por nombre completo, sin `id`), que es el que acepta un consumidor tipo RomCenter/Retool (no interpretan `id`/`cloneofid`). No aplica curación propia (no descarta Proto/Demo/Beta/etc., eso es trabajo de Retool aguas abajo); namespacea los `id` por fichero de origen para fusionar varias entradas sin colisión. El árbol de clones que produce es el mismo que ya trae el DAT de origen (`id`/`cloneofid` asignados por No-Intro) — el script solo traduce de esquema, no recalcula parentesco. |
| `generate-parent-clone-fullset.ps1` / `generate-parent-clone-fullset.py` | Driver de lote sobre el anterior: aplica `convert-cloneofid-to-parent-clone.ps1`/`.py` a `full`+`aftermarket` de **todos** los sistemas del manifiesto `config/nointro-systems.json` (o uno solo con `-SystemId`/`--system-id`), generando el DAT Parent-Clone de cada uno en `sources/dats/no-intro/fullset/` — la entrada real de Retool. Paso siguiente a `update-sources.ps1` en el flujo real, no un sustituto: primero `update-sources.ps1` sincroniza `sources/dats/no-intro/{pc,full,aftermarket}/` desde `metadata/dat/No-Intro/`, después este script convierte `full`+`aftermarket` al esquema que Retool necesita. |

## Filtrado 1G1R (Retool)

| Script | Propósito |
| --- | --- |
| `check-collection-vs-1g1r.py` | **Solo Python.** Compara una colección real de ROMs/discos contra los 3 DAT de `retool-1g1r-pipeline.py` (FULLSET, 1G1R FINAL, cuarentena) en dos pasadas: coincidencia exacta por nombre, y coincidencia normalizada (título base + región + tag de disco/lado si aplica, ignorando idioma/revisión/rerelease) para separar "de verdad no lo tienes" de "lo tienes pero Redump lo renombró" — este segundo caso es frecuente en colecciones grandes y sin esta distinción se confunde con huecos reales. Puede además: organizar los matches en carpetas `fullset/1g1r/japan` (`--output-fullset`/`--output-1g1r`/`--output-japan`, copy/move/symlink/hardlink); generar un CSV de renombrado propuesto (`--rename-plan`) a partir de los matches normalizados, **para revisar a mano antes de aplicarlo** con `--apply-renames` (nunca renombra ni sobrescribe sin ese paso explícito; filas con conflicto — mismo fichero, propuestas distintas entre capas — se marcan y se saltan); generar un CSV de faltantes reales (`--missing-report`, columnas `tier,title,expected_filename` — usa `--ext` para que el nombre esperado incluya extensión) por cada capa comparada, pensado como lista de descarga pendiente directamente accionable; y, con `--raw-dat` (el DAT Redump/No-Intro sin filtrar, antes del paso 1 de retool), separar los ficheros "sobrantes" de FULLSET en **excluidos a propósito** (demo/proto/aftermarket/unlicensed/multimedia — están en el catálogo completo pero el paso 1 los saca por diseño) frente a **desconocidos de verdad** (ni siquiera están en el catálogo sin filtrar — fichero mal nombrado, región no catalogada por Redump, u otra fuente); volcable a CSV con `--excluded-report` (columnas `category,filename`). |
| `tosec-nonstandard-labels.py` | **Solo Python.** Escanea uno o varios DAT TOSEC y clasifica cada etiqueta entre paréntesis/corchetes contra el vocabulario **oficial** de TOSEC (región/idioma/vídeo/copyright/estado de desarrollo/flags de dump, ver `docs/references.md#tosec`), sacando la frecuencia de las que NO encajan — diagnóstico previo a Igir, ver nota en `1g1r-filtering.md#igir`: el `DATParentInferrer` de Igir solo despoja el vocabulario oficial al agrupar 1G1R, así que una etiqueta de escena no oficial (`[k-file]` en `atari800`, `[CPM Version]`/`[master disk N]`/`[gunstick]` en `amstradcpc`, ambas confirmadas con este script) impide que Igir fusione variantes que en la práctica son el mismo contenido. Solo diagnóstico, no modifica nada — cada etiqueta que saca hay que revisarla a mano (algunas son ruido legítimo: requisito de hardware como `(130XE)`/`(OS-B)`, no duplicados). |
| `retool-1g1r-pipeline.py` | **Solo Python** (Retool no tiene binario Windows nativo, requiere el devcontainer — mismo motivo que `compare-redump-mameredump.py`). Automatiza el pipeline de 4 pasadas encadenadas con `--reprocess` documentado en `docs/guides/tools/1g1r-filtering.md#pipeline-recomendado--varias-pasadas-encadenadas-con---reprocess`: FULLSET (excluye todo lo no-oficial) → 1G1R-BASE (región+compilaciones) → INTERMEDIATE (superconjunto de idiomas) → FINAL + cuarentena (`--removesdat`). Recibe el DAT Parent-Clone oficial **sin modificar** (ej. `sources/no-intro/<Sistema> (Parent-Clone) (*).dat`) — no inyecta `region`/`language` propios: Retool los deriva del nombre del título por sí solo (confirmado leyendo `modules/dat/process_dat.py` del propio Retool), así que un conversor previo no aporta nada ahí. Ver la cabecera del fichero para el detalle completo de parámetros. **Configuración necesaria antes de usarlo** (ver más abajo). |

**Configuración que requiere `retool-1g1r-pipeline.py`, no incluida en el repo:**

1. **Docker Desktop corriendo** y la imagen `emulation-devcontainer` ya construida: `docker build -t emulation-devcontainer .devcontainer/` (contiene Retool + SabreTools, ver `.devcontainer/Dockerfile`).
2. **Copia local de `retool-clonelists-metadata`** con subcarpetas `clonelists/`, `metadata/`, `mias/`, `retroachievements/` — el valor por defecto de `--clonelist-dir` apunta a `sources/retool-clonelists-metadata/input/` (el clon git completo de `unexpectedpanda/retool-clonelists-metadata`, no la copia curada de nivel superior de `sources/retool-clonelists-metadata/`, que hoy no cubre todos los sistemas — ver `docs/dat-sources.md`). Si esa carpeta no existe todavía en tu entorno, clonar `https://github.com/unexpectedpanda/retool-clonelists-metadata` ahí o pasar `--clonelist-dir` a donde la tengas.
3. **DAT de entrada**: el Parent-Clone oficial de DAT-o-MATIC tal cual, sin pasar por ningún conversor propio (`sources/no-intro/<Sistema> (Parent-Clone) (*).dat`, sincronizado por `update-sources.ps1`/`.py`).
4. **Nombres de idioma exactos**: `--keep-languages`/`--quarantine-languages` deben usar los nombres tal como los espera Retool en `language order:` (ej. `Spanish (Latin American)`, no `es`) — la lista cerrada vive en `KNOWN_LANGUAGE_ORDER` dentro del propio script, copiada de la plantilla que genera Retool en su primer arranque; si una versión futura de Retool cambia esa lista, hay que actualizarla ahí a mano.
5. **Opcional, recomendado:** `--retool-config-dir` apuntando a una carpeta persistente propia (no la temporal por defecto dentro de `--output`) si vas a procesar varios sistemas — evita repetir el bootstrap de `config/internal-config.json` (única llamada de red del pipeline; el clonelist/metadata no se descarga porque ya viene montado desde el punto 2).

## Construcción de romsets (`data/roms/`, colección física)

| Script | Propósito |
| --- | --- |
| `create-roms-structure.ps1` | Crea la estructura de carpetas destino en `data/roms/` (arcade/console/micro por identificador canónico), con stub de `gamelist.xml` y carpeta `media/`. |
| `build-complete-romset.ps1` | Construye un romset "completo" de un sistema aplanando raíz + subcarpetas válidas (jpn/unl/pack/etc.) más `_extra/`, excluyendo carpetas de trabajo (`_SD`, `duplicates`, `raw`). |
| `promote-complete-romset.ps1` | Promociona el romset "completo" generado por el script anterior a la carpeta final, sustituyendo el contenido previo (modo simulación por defecto; requiere `-Execute`). |
| `strip-retool-tag.ps1` | Renombra ficheros quitando el sufijo `(Retool ...)` que retool añade tras el nombre base. |

## Fuentes de referencia (catálogos externos)

| Script | Propósito |
| --- | --- |
| `launchbox-platform-games.py` | **Solo Python.** Consulta `LaunchBox.Metadata.db` (SQLite local que LaunchBox genera a partir de su `Metadata.zip`, ver `docs/guides/romsets/systems/README.md#fuentes-de-referencia--más-allá-de-wikipedia`) para listar los juegos de un sistema por su `Platform` — alternativa a scrapear la web (sin API oficial) o parsear a mano el XML de ~500 MB del bulk download. `--system <id>` resuelve el nombre de plataforma vía `PLATFORM_MAP` (id canónico de `docs/systems.md` → `Platforms.Name`, verificado 1:1 contra la base real el 2026-09-06: 76 sistemas mapeados, 7 sin plataforma equivalente en LaunchBox — `dsiware`/`ndsi`/`newn3ds`/`psn`/`sgb`/`sufami`/`thomson`); `--list-platforms` vuelca todos los nombres reales para verificar/ampliar el mapeo antes de asumir uno nuevo. `--exclude-nonjuego` descarta `Homebrew`/`ROM Hack`/`Unlicensed` (mismo criterio de "catálogo oficial" ya aplicado a No-Intro/Redump/TOSEC en este repo). Salida CSV (`DatabaseID,Name,ReleaseYear,ReleaseDate,ReleaseType,Genres,Developer,Publisher`) a fichero o stdout. Requiere tener LaunchBox instalado con la base sincronizada (`--db` para apuntar a otra ruta si no coincide con `F:\tools\LaunchBox\Metadata\LaunchBox.Metadata.db`). |
| `thegamesdb-platform-games.py` | **Solo Python.** Consulta la API oficial de TheGamesDB (`api.thegamesdb.net`, alta de credencial en `docs/guides/tools/api-credentials.md`) vía `GET /v1/Games/ByPlatformID`, resolviendo la plataforma por nombre en tiempo real (`--platform-name`, vía `/v1/Platforms/ByPlatformName`) o por id directo (`--platform-id`) — a diferencia de `launchbox-platform-games.py`, no lleva un mapeo id-canónico→plataforma hardcodeado porque no se pudo verificar contra la API real sin tener antes una clave; `--list-platforms` vuelca id+nombre reales para resolverlo a mano la primera vez. **Escrito el 2026-09-06 sin API key disponible, no probado todavía contra una respuesta real** — construido contra el esquema Swagger documentado (`github.com/api-evangelist/thegamesdb`) y el formato de paginación (`data`/`pages`/`remaining_monthly_allowance`) ya conocido por la comunidad; validar `_unwrap_games()`/`_unwrap_platforms()` en cuanto haya una clave real, antes de confiar en la salida. Maneja paginación completa (`pages.next`) y respeta el límite mensual con una pausa entre páginas. Salida CSV (`id,game_title,release_date,platform,overview,genres,publishers,rating`) a fichero o stdout; clave también aceptada por variable de entorno `THEGAMESDB_API_KEY`. |
| `giantbomb-platform-games.py` | **Solo Python.** Mismo patrón que `thegamesdb-platform-games.py`, contra la API oficial de GiantBomb (`www.giantbomb.com/api/`, alta en `docs/guides/tools/api-credentials.md`). Resuelve la plataforma por nombre en tiempo real (`GET /platforms/?filter=name:<nombre>`) para obtener su `id` **numérico** propio — GiantBomb identifica cada recurso también por un `guid` (string), pero el filtrado de `GET /games/` por plataforma usa el `id` numérico (`filter=platforms:<id>`), no el guid; confirmado vía foro oficial y wrappers de terceros, no en la documentación Swagger propiamente. `--list-platforms`/`--platform-name`/`--platform-id` igual que en TheGamesDB. **Escrito el 2026-09-06 sin API key disponible, no probado todavía** — envía un `User-Agent` propio (GiantBomb lo pide explícitamente en vez del genérico de la librería HTTP) y respeta el límite de 200 peticiones/recurso/hora con una pausa entre páginas de paginación por `offset`. Salida CSV (`id,guid,name,original_release_date,deck,genres`) a fichero o stdout; clave también aceptada por variable de entorno `GIANTBOMB_API_KEY`. |
| `worldofspectrum-games.py` | **Solo Python. Específico de ZX Spectrum** (no multi-sistema como los demás). Consulta la API REST JSON en vivo de World of Spectrum (`worldofspectrum.org/infoseek/api/`), que en realidad es un frontend sobre **ZXDB** (confirmado: WoS fue relanzado en 2020 usando ZXDB como backend) — mismo dato que el dump MySQL de `github.com/zxdb/ZXDB`, pero accesible sin levantar un servidor, vía API en vivo. **Probado contra la API real** (2026-09-06): funciona con `X-API-KEY=test` sin registro visible; `--title`/`--entry-group` filtran, `--all` pagina el filtro completo. **Tope de paginación duro confirmado en el propio servidor: 10 resultados/página** pase lo que pase en `limit` (probado con 500 y 5000, ambos recortados a 10) — traer el catálogo completo (24.368 títulos, 10.894 solo `entry_group=games`) exige miles de peticiones por `offset`, de ahí el aviso en `--all` para acotar primero con `--title`/`--entry-group`. Salida CSV (`id,title,entry_type_text,entry_group,no_players,availability_text,publishers,features,controls`) a fichero o stdout. |
| `openvgdb-platform-games.py` | **Solo Python.** Consulta `openvgdb.sqlite` (SQLite descargable de `github.com/OpenVGDB/OpenVGDB/releases/latest`, sin API/credencial — descarga manual, `--db` apunta al fichero) — mismo patrón que `launchbox-platform-games.py`. **Fuente parada, no viva**: última release (v29.0) de noviembre de 2021, sin nada posterior; solo tiene sentido como snapshot legado, no para catálogo reciente. Su valor diferencial: incluye **hashes CRC/MD5/SHA1 por ROM** junto con título/desarrollador/editora/género — como RetroAchievements, pero con hashes de fichero estándar en vez del algoritmo propio de RA. `--system <id>` resuelve vía `PLATFORM_MAP` (id canónico → `SYSTEMS.systemName`), verificado 1:1 contra la base real el 2026-09-06 **incluyendo conteo de filas reales**, no solo presencia en la tabla: 33 sistemas con datos reales; `3do`/`jaguarcd`/`pcfx`/`c64` figuran en `SYSTEMS` pero tienen 0 filas reales en `ROMs`/`RELEASES` (placeholders sin datos, detectado probando el script contra la base real antes de darlo por bueno — la tabla `SYSTEMS` por sí sola habría sido engañosa) y se bloquean explícitamente en vez de devolver una lista vacía silenciosa. `--list-systems` vuelca todos los `systemID`+nombre reales. Salida CSV (`releaseTitleName,releaseDeveloper,releasePublisher,releaseGenre,releaseDate,romFileName,romHashCRC,romHashMD5,romHashSHA1`) a fichero o stdout. **Probado contra la base real** (a diferencia de TheGamesDB/GiantBomb/IGDB/RetroAchievements, aquí sí había datos disponibles sin credencial). |
| `retroachievements-platform-games.py` | **Solo Python.** Mismo patrón, contra la API oficial de RetroAchievements (`retroachievements.org/API`, alta en `docs/guides/tools/api-credentials.md`) vía `API_GetGameList.php`. Cobertura limitada a sistemas que RA soporta con logros — no es un catálogo genérico como los otros cuatro, pero es la **única fuente que devuelve hashes MD5 junto con el listado** (`--include-hashes`, parámetro `h=1`), conectando con `rahashes`/`unofficial-ra-dats` ya catalogados en `docs/dat-sources.md` para verificación de CHD; también admite `--only-with-achievements` (`f=1`, descarta entradas sin logros publicados). Resuelve la consola por nombre en tiempo real (`API_GetConsoleIDs.php`) o acepta `--console-id` directo; `--list-consoles` para explorar. Autenticación con usuario + clave (ambos exigidos en cada petición, a diferencia de las otras APIs que solo piden clave) — `--api-key`/`--username` o `RA_API_KEY`/`RA_USERNAME`. **Escrito el 2026-09-06 sin API key disponible, no probado todavía.** Salida CSV (`ID,Title,NumAchievements,Points,DateModified,Hashes`) a fichero o stdout. |
| `igdb-platform-games.py` | **Solo Python.** Mismo patrón, contra la API oficial de IGDB (`api.igdb.com/v4`, alta en `docs/guides/tools/api-credentials.md`). A diferencia de las otras tres, existe ya una vía cubierta con herramienta de comunidad (módulo `igdb` de `SkyScraper`, ver `docs/tools.md`) — se hizo este script propio de todos modos porque SkyScraper solo scrapea por **emparejamiento contra una romset ya presente en disco** (nombre de fichero → búsqueda), así que no sirve como fuente de referencia previa a tener los ficheros; este script consulta el catálogo completo de una plataforma de forma independiente, igual que los otros tres. Autenticación de app de Twitch (`--client-id`/`--client-secret` o `IGDB_CLIENT_ID`/`IGDB_CLIENT_SECRET`), token de aplicación pedido en cada ejecución (no cacheado). Consultas en lenguaje Apicalypse contra `POST /v4/platforms`/`POST /v4/games`; resuelve plataforma por nombre vía `search` exigiendo coincidencia exacta (evita que la búsqueda difusa de IGDB cuele un resultado equivocado) o acepta `--platform-id` directo. **Escrito el 2026-09-06 sin credenciales disponibles, no probado todavía.** Respeta el límite de 4 peticiones/segundo con una pausa entre páginas. Salida CSV (`id,name,first_release_date,summary,genres`) a fichero o stdout. |

## Documentación

| Script | Propósito |
| --- | --- |
| `generate-romset-docs.ps1` | Genera/actualiza la sección auto-generada de cada `docs/guides/romsets/systems/<id>.md` a partir del índice correspondiente, y regenera la tabla índice de `systems/README.md`. |

## Scripts de un solo uso

| Script | Propósito |
| --- | --- |
| `add-wikipedia-refs.ps1` | Inserta la sección "Fuentes de referencia" con enlace de Wikipedia en cada `docs/guides/romsets/systems/<id>.md`. Ya ejecutado; no forma parte del pipeline habitual. **Superado por `add-wikipedia-refs.py`** (ver abajo) para el enlace de tipo "List of ... games", que es un formato de URL distinto (con encoding no trivial) al enlace genérico al artículo del sistema que generaba este script. |
| `fix-wikipedia-refs-blank-line.ps1` | Corrige la falta de línea en blanco tras la inserción de `add-wikipedia-refs.ps1`. Ya ejecutado. |
| `add-wikipedia-refs.py` | **Solo Python.** Inserta o rellena la sección "Fuentes de referencia" con el enlace a la lista de juegos de Wikipedia (`List of <Sistema> games`, más específico y útil para curación que el artículo genérico del sistema que insertaba `add-wikipedia-refs.ps1`), calculando el encoding de la URL en vez de escribirlo a mano — motivo del script: el encoding manual (espacios → `_`, `&` → `%26`, guion en-dash `–` → `%E2%80%93`...) era la causa de errores de formato repetidos al añadirlos a mano. Genera el **formato canónico fijado el 2026-09-06**: heading `## Fuentes de referencia` + `- Wikipedia:` + sub-lista anidada, una URL por línea (incluso si solo hay una). El mapeo `id → título(s) de artículo` va hardcodeado en `WIKI_MAP` dentro del propio script (patrón de script de un solo uso/por lotes, como los anteriores) — verificar cada título nuevo contra `https://en.wikipedia.org/wiki/Category:Lists_of_video_games_by_platform` antes de añadirlo, no inventar un artículo que no esté confirmado ahí (ojo, esa categoría tampoco es infalible — `List_of_Commodore_128_games` resultó no ser un artículo real de C128 pese a "existir" la URL, ver `docs/session-context.md`). Soporta un valor especial `TODO` para sistemas sin artículo de lista específico confirmado (deja una nota `[TODO]` en vez de forzar un artículo aproximado). Idempotente: si la sección ya existe (en cualquiera de los formatos históricos, ver `normalize-wikipedia-refs.py`), la reemplaza; si no existe, la inserta tras el título. Ejecutado 2026-09-06 para 22 sistemas (14 con artículo confirmado, 8 con `[TODO]`, luego completados a mano por el usuario). |
| `normalize-wikipedia-refs.py` | **Solo Python, ya ejecutado.** Convierte las tres variantes de formato que llegaron a convivir en el corpus (heading + bullet plano; heading + sub-lista anidada; sub-lista anidada sin heading) al formato canónico único (heading + sub-lista anidada). No reescribe ninguna URL, solo reestructura. Ejecutado 2026-09-06 sobre los 84 ficheros de `docs/guides/romsets/systems/`; verificado 84/84 en formato consistente tras la pasada. |
| `test-wiki-match-gamegear.ps1` | Prueba de emparejamiento de títulos de Wikipedia contra `metadata/dat-index/gamegear.json`; script exploratorio, no reutilizable directamente. |
| `build-fuentes-referencia-table.py` | **Solo Python, ya ejecutado.** Convierte la sección "Fuentes de referencia" de los 84 `docs/guides/romsets/systems/<id>.md` del formato de bullets anidados al formato tabla `\| Tipo \| Fuente \| Detalle \|` (heading + filas `Especialista`/`Genérica`), volcando `SPECIALIST_MAP` (resultado de la investigación de fuentes especialistas por sistema, sesión 2026-09-06) y reformateando la fila de Wikipedia ya existente. **Idempotente desde el fix del 2026-09-06** (detecta si la sección ya es una tabla y no la reprocesa) — antes de ese fix, relanzarlo sobre su propia salida borraba la fila Wikipedia en los 84 ficheros (ver `repair-fuentes-referencia-wikipedia.py`). |
| `repair-fuentes-referencia-wikipedia.py` | **Solo Python, ya ejecutado, reparación puntual de un incidente.** Restaura la fila `Genérica \| Wikipedia` perdida por el relanzamiento accidental de `build-fuentes-referencia-table.py` sobre su propia salida: recupera la URL exacta desde `git show HEAD:<path>` para los ficheros ya versionados; para los ficheros nuevos sin HEAD, solo restaura lo que se conoce con certeza (`RECOVERED_KNOWN` — generado por `add-wikipedia-refs.py` o visto directamente en la conversación) y deja `` `[TODO]` `` con el motivo explícito en los demás (`UNRECOVERABLE`) en vez de inventar una URL. Documentado como referencia de qué pasó, no pensado para reutilizarse. |
